"""
URLs for embeddings app

Note: URLs are now handled directly in admin.py
This file is kept for backwards compatibility.
"""
from django.urls import path

app_name = 'embeddings'

urlpatterns = []
