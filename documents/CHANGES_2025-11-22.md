# تغییرات 22 نوامبر 2025

## ✅ تغییرات اعمال شده و تست شده:

### 1. تغییر label فیلد تاریخ نسخه
- **فایل:** `/srv/ingest/apps/documents/forms.py`
- **تغییر:** "تاریخ نسخه (شمسی)" → "تاریخ نسخه/تصویب"
- **خط:** 34
- **وضعیت:** ✅ تست شده و کار می‌کند

### 2. رفع مشکل validation فیلد parent در LegalUnit
- **فایل:** `/srv/ingest/apps/documents/forms.py`
- **مشکل:** خطای "یک گزینه معتبر انتخاب کنید" هنگام انتخاب parent
- **راه‌حل:** 
  - تنظیم `queryset = LegalUnit.objects.all()` در `__init__`
  - انجام validation واقعی در `clean_parent`
- **خطوط:** 155-175, 177-197
- **وضعیت:** ✅ تست شده با 3 سناریو مختلف

### 3. سفارشی‌سازی فیلد password در User Admin
- **فایل:** `/srv/ingest/admin.py`
- **تغییر:** نمایش فقط لینک "تغییر رمز عبور" بدون hash و متن راهنما
- **روش:** 
  - جایگزین کردن فیلد `password` با `password_display`
  - متد `password_display` فقط لینک را نشان می‌دهد
- **خطوط:** 161-201
- **وضعیت:** ✅ تست شده و کار می‌کند

## 📋 فایل‌های تغییر یافته:

1. `/srv/ingest/admin.py` (8.8K)
2. `/srv/ingest/apps/documents/forms.py` (12K)
3. `/srv/ingest/apps/accounts/admin.py` (7.6K - تغییرات قبلی)

## ⚠️ نکات مهم:

### بعد از هر restart container:
این فایل‌ها از host به container کپی شده‌اند و در container موجودند.
اما چون volumes به صورت مستقیم mount نشده‌اند، بعد از هر rebuild image باید دوباره کپی شوند.

### برای جلوگیری از از دست رفتن تغییرات:
1. همیشه تغییرات را در host (`/srv/ingest/`) انجام دهید
2. بعد از تغییر، فایل را به container کپی کنید
3. container را restart کنید
4. یا بهتر است image را rebuild کنید:
   ```bash
   cd /srv/deployment
   docker compose -f docker-compose.ingest.yml build web
   docker compose -f docker-compose.ingest.yml up -d
   ```

## 🧪 تست‌های انجام شده:

✅ CustomUserAdmin با password_display
✅ LegalUnitForm با parent validation
✅ InstrumentExpressionForm با label جدید
✅ همه فایل‌ها در host و container sync هستند

## 📅 تاریخ: 2025-11-22 08:19 UTC
